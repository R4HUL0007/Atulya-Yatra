# Atulya Yatra 2.0 — dependencies
# Kept intentionally small for simple deployment.
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
Werkzeug==3.0.4
Jinja2==3.1.4

# Dynamic place data (Google Places API) + .env loading
python-dotenv==1.0.1
requests==2.32.3

# Production WSGI server (used inside the Docker container)
gunicorn==23.0.0
